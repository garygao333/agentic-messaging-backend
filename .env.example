# ---- Server ----
PORT=8787

# ---- Supabase (shared DB) ----
SUPABASE_URL=https://gsadcteyuplycuwskvru.supabase.co
# Service-role SECRET key (server-side only). Get from Supabase → Settings → API.
SUPABASE_SERVICE_KEY=
# Stopgap: the publishable/anon key works for the MVP because RLS is permissive.
# Used ONLY if SUPABASE_SERVICE_KEY is empty (with a loud warning).
SUPABASE_ANON_KEY=

# ---- LLM ----
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ---- 1440 MSP (Apple Messages for Business) ----
MSP_API_BASE=https://msp.1440.co/functions/v1
MSP_API_KEY=
MSP_BUSINESS_ID=914e49f4-2b03-4e2b-987f-8c8f45e40294
# If 1440 signs inbound webhooks with a per-subscription secret, set it here (TBC).
MSP_WEBHOOK_SECRET=

# ---- App → backend auth ----
# Interim MVP: a shared bearer token the app sends as `Authorization: Bearer <token>`.
APP_SHARED_TOKEN=
